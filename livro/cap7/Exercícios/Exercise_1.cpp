#include <iostream>

using namespace std;

/**
 This exercise is just for practice macros!
 */

#define ABS(a) ( (a >= 0) ? a : (-a))

#define MAX(a,b) ( (a >= b) ? a : b)

#define MIN(a,b) ( (a <= b) ? a : b)

